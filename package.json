{
  "name": "fastify-todo-app",
  "version": "1.0.0",
  "description": "Fastify todo app with clean architecture",
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf dist && tsc -p tsconfig-build.json",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.2",
    "@types/node": "^18.0.0",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  }
}
